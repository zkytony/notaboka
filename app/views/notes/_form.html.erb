<%= form_for @note, url: url  do |f| %>
  <% if @note.errors.any? %>
    <div id="error_explanation" class="error-msg">
      <h2><%= pluralize(@note.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
      <% @note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :importance %>
    <%= f.select :importance, options_for_select([1, 2, 3, 4, 5]) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
